# AIive 代码审查报告

> 审查范围：后端 `backend/aiive/`（api / runtime / worker / tools / core / memory / db / mcp / knowledge / selfdev / storage / supervisor）
> 审查方式：逐文件静态阅读 + 多模块交叉核对（agent_graph.py、builtin_tools.py、db/models.py 调用关系）。
> 说明：**仅审查、未修改任何代码**。本章节中标记为「已核实」的条目经主代理直接复读源码确认；标记为「需确认」的条目由分模块审查代理发现，主代理未逐行复读，可信度较高但建议落地前再确认一次。

---

## 一、高危问题（已核实，强烈建议优先修复）

### 1. `safe_delete.py` 仓库根目录计算多上一层（安全 + 逻辑缺陷）
**文件**：`backend/aiive/tools/safe_delete.py:56`
```python
REPO_ROOT = Path(__file__).resolve().parent.parent.parent.parent.parent
```
文件位于 `backend/aiive/tools/safe_delete.py`，从 `__file__` 向上数：
- `.parent`×1 = `tools`
- ×2 = `aiive`
- ×3 = `backend`
- ×4 = `AIive`（仓库根）
- ×5 = **仓库根的上一级目录**（多了一次）

**后果**：
- 默认 scope 注册（`REPO_ROOT/"tests"/"artifacts"`、`REPO_ROOT/".data"/"object_store"`）全部解析到错误位置（仓库外一级），安全删除实际指向错误目录。
- 危险路径 `DANGEROUS_PATHS` 中的 `str(REPO_ROOT)` 与 `str(REPO_ROOT/"backend")` 也偏移到错误层级，导致对**真实**仓库路径的保护完全失效——安全删除的防护形同虚设。
- 该模块是 `safe_delete` 高权限工具的底层（builtin_tools 注册为 `risk_level="high"`），属于安全关键代码。

**建议**：改为 4 个 `.parent`，或改用 `Path(__file__).resolve().parents[3]` 并加注释；建议补充单元测试断言 `REPO_ROOT.name == "AIive"`。

---

### 2. `task_worker.py` 模块级函数引用未定义变量 `task`（导致提醒功能静默失效）
**文件**：`backend/aiive/worker/task_worker.py:109-201`
```python
def _wake_agent_for_reminder(task_id: str, task_title: str, target_thread_id: str):
    ...
    agent_db.commit()
    logger.info(
        "提醒 Agent 唤醒成功: task_id=%s title=%s thread_id=%s",
        task.id, task.title, target_thread_id,   # ← task 不存在，函数参数只有 task_id/task_title
    )
    ws_manager.broadcast_to_thread_sync(...)      # ← 这两行因上一行 NameError 永远执行不到
    ...
    except Exception:
        logger.exception(
            "提醒 Agent 唤醒失败，回退为 notification_created: task_id=%s title=%s",
            task.id, task.title,                  # ← 同样 NameError
        )
        fallback_db = SessionLocal()
        EventLogger(fallback_db).log_event(
            ...
            "task_type": task.task_type,          # ← 同样 NameError
        )
```
**后果（严重）**：
- 提醒到期、Agent 成功生成回复并 `commit()` 后，执行 `logger.info` 时抛出 `NameError`，被外层 `except` 捕获；异常分支里再次 `NameError`，于是：
  1. **用户永远收不到 websocket 推送的提醒**（`broadcast_to_thread_sync` 在 `logger.info` 之前，因 `NameError` 未执行）；
  2. 回退写入 `notification_created` 事件也因 `NameError` 失败，最终仅打印「回退事件写入失败」。
- 即：提醒功能"看起来执行了，实际用户侧完全无感知"，且日志表现为误导性异常。

**根因**：实例方法 `_wake_agent_for_reminder(self, task, target_thread_id)`（104 行）拆出 `task.id/task.title` 传给模块级函数，但模块级函数体内仍按 `task` 对象访问字段。设计不一致。

**建议**：模块级函数统一接收 `task` 对象（或统一用 `task_id/task_title` 并修正所有引用）；补充单测覆盖"提醒成功唤醒"路径。

---

### 3. `policy_engine.py` 未知/被阻止工具在同一批次含"允许"工具时被悄悄放行
**文件**：`backend/aiive/runtime/policy_engine.py:127-151`
```python
# 确定整体策略动作
if blocked and not allowed and not confirm:
    return PolicyResult(action=PolicyAction.BLOCK, ...)

if confirm:
    return PolicyResult(action=PolicyAction.CONFIRM, ...)

return PolicyResult(action=PolicyAction.ALLOW, ...)
```
**后果（安全）**：
- 当批次中同时出现 `blocked`（如未知/未注册工具）和 `allowed`（任意正常工具）时：`blocked and not allowed` 为 False → 跳过 BLOCK；`confirm` 为空 → 跳过 CONFIRM；最终落到 `return ALLOW`。
- `agent_graph._policy_check` 只在 `action == PolicyAction.BLOCK` 时 `finalize`，其余一律 `continue` 进入 `ToolNode` 执行 **全部** tool_calls。因此**未知工具被实际执行**，且 `blocked_tools` 仅作为字段返回、未阻止执行。
- `CONFIRM` 也存在同样问题：被标记 `confirm` 的工具与 `blocked` 工具混批时，`blocked` 工具既不在 `confirm_tools` 也不在 `allowed_tools`，但最终 `action != BLOCK`，依旧执行。

**文档与实现矛盾**：注释/文档承诺"未知工具则阻止"，且"同批含允许与确认则阻止整批次"——实际两种承诺都未实现。

**建议**：在 `if confirm:` 分支之前先处理 `blocked`；无论 `allowed/confirm` 是否存在，只要存在 `blocked` 就返回 `BLOCK`。或将 `blocked_tools` 真正从可执行的 tool_calls 中剔除。

---

### 4. `memory_write_service.py` supersede 同时入队向量 upsert 与 delete（新记录无法被检索）
**文件**：`backend/aiive/memory/memory_write_service.py:498, 677-709`
```python
# _execute_supersede_or_revise 中：
self._enqueue_projection(new_record, "memory.superseded", invalidate_cache=True)

# _enqueue_projection 中：
if event_type in ("memory.created", "memory.reinforced", "memory.superseded",  # ← 命中 upsert
                  "memory.revised", "memory.merged", "memory.wake"):
    if not is_candidate:
        self._db.add(OutboxJob(... job_type="memory_vector_upsert" ...))
...
if event_type in ("memory.forgotten", "memory.sleep", "memory.archived",
                  "memory.superseded"):                                          # ← 再次命中 delete
    self._db.add(OutboxJob(... job_type="memory_vector_delete" ...))
```
**后果（数据一致性）**：对 `new_record` 传入 `"memory.superseded"`，该 event_type 同时出现在 upsert 列表与 delete 列表，导致**同一条新记录既入队 `memory_vector_upsert` 又入队 `memory_vector_delete`**。最终新记录向量被删除 → **新记忆无法在向量库被检索到**。同时旧的被替代记录并未单独入队 delete，其旧向量未被清理（残留）。语义完全相反。

**建议**：supersede 应分别为 `new_record`（`memory.created`/upsert）与 `old_record`（`memory.superseded`/delete）入队，而非对同一个 `new_record` 用 `"memory.superseded"` 同时触发两条冲突任务；或将 `"memory.superseded"` 从 upsert 列表中移除。

---

## 二、代码逻辑错误（需确认 / 已核实）

### 5. `routes_notifications.py` 物理删除 Event 但关联 Task 残留（提醒会再次弹出）
**文件**：`backend/aiive/api/routes_notifications.py:69-85`
```python
@router.delete("/notifications/{notification_id}")
def delete_notification(notification_id, db=Depends(get_db)):
    event = db.get(Event, notification_id)
    if event is None:
        return {"ok": False, "error": "通知不存在"}
    db.delete(event)
    db.commit()
```
**问题**：直接物理删除 `reminder_created` 事件，但对应 `tasks` 表中的 `Task`（状态仍为 `pending`）未被清理。后台调度守护进程到期仍会触发该 Task，已"删除"的通知会再次弹出。与 `builtin_tools._handle_cancel_task`（标记 `cancelled`）语义混用。

### 6. `routes_chat.py` 同步 `/chat` 的 rollback 作用于错误会话
**文件**：`backend/aiive/api/routes_chat.py:48-64`
```python
def chat(request, db=Depends(get_db)):
    try:
        result = invoke_chat(message=request.message, thread_id=...)
    except LLMClientError:
        db.rollback()
        raise
```
**问题**：`invoke_chat`（runtime/graph.py）内部使用**独立的 `SessionLocal()`** 会话，与路由注入的 `db` 不是同一会话。`LLMClientError` 发生时回滚的是路由的"空"会话，对 `invoke_chat` 内部真正写入的会话无效，可能遗留悬挂事务。且同步接口抛 500、流式 `chat_stream` 返回 SSE `error` 事件，同一 `/api/chat` 功能两套错误表达，前端需分别处理。

### 7. `action_planner.py` `plan()` 返回的决策对象字段缺失（若被启用将误导）
**文件**：`backend/aiive/core/action_planner.py:119-129`
`AgentDecision` 的 `memory_signal`、`tool_params`、`args`、`requires_confirmation` 等字段从未赋值（仅 `should_execute=False`、`tool_name=None`）。当前 `plan()` 未被调用，但作为对外 API 信息严重不足。
> **状态**：已确认无影响，不修复。所有未赋值字段在 Pydantic 模型中均有合法默认值；`plan()` 全代码库零调用（已于问题 15 中删除该死代码）。

### 8. `attention_manager.py` 时区比较不一致（需确认）✅ 已修复
**文件**：`backend/aiive/runtime/attention_manager.py:69`
```python
created = last_state.created_at.replace(tzinfo=timezone.utc) if last_state.created_at.tzinfo is None else last_state.created_at
```
**问题**：仅当 `tzinfo is None` 时补 UTC，否则原样使用。若 DB 返回带非 UTC 时区的 aware datetime，会直接与 UTC 的 `now` 相减，得到错误的时间差。建议统一转换为 UTC 再比较。
> **修复**：naive datetime → `replace(tzinfo=utc)`；aware datetime → `astimezone(utc)` 统一转 UTC。

### 9. `memory/automatic_recall.py` `_scope_score` 在重复 scope_type 下命中错误位置（需确认）✅ 已修复
**文件**：`backend/aiive/memory/automatic_recall.py:80`
```python
idx = [s[0] for s in chain].index(actual_scope)
```
**问题**：`chain()` 可能含多个相同 `scope_type`（多个 capability），`index()` 只返回第一个匹配下标。若记录实际属于第二个 capability，其 scope 评分会被算成第一个 capability 的位置，导致多 capability 场景下排序/打分错误。建议用 `(scope_type, scope_id)` 二元组匹配。
> **修复**：改为 `(scope_type, scope_id)` 精确二元组匹配 + `scope_type` 回退匹配，调用处传入 `r.scope_id`。

### 10. `memory/memory_types.py` `compute_content_hash` 与注释/去重语义不符（需确认）
**文件**：`backend/aiive/memory/memory_types.py:349-352`
注释声明 `sha256(normalized_content)`，实际对 `self.content` 取哈希；若 `conflict_resolver` 依赖 `content_hash` 去重/强化，则不同空白/大小写但语义相同的记忆会被判为不同 hash，可能误 `create` 而非 `reinforce`。
> **状态**：已确认无实际影响，不修复。注释有误导（写了 `normalized_content` 但未实现正规化），但 `conflict_resolver._hash_matches()` 中 DB 记录的 hash 也是用完全相同的 `sha256(content)` 算法计算，两侧一致，相同内容必然匹配。缺少正规化属于功能增强，非 bug。

### 11. `memory/memory_write_service.py` reinforce 路径无 source_event 时仍 +1 置信度（需确认）
**文件**：`backend/aiive/memory/memory_write_service.py:430-431`
`proposal.source_event_ids` 为空时 `new_count = 1`，即便没有新证据也会给记录 +`trust_mult` 并执行 `reinforce_count += 1`；"无新事件不应强化"的语义被破坏。
> **状态**：已确认非 bug，不修复。`new_count = 1` 是有意设计：当无 source_event 可去重时，proposal 本身算 1 次强化证据（`trust_mult = 0.05` 影响极小）。此路径仅在 `ConflictResolver` 已判定应 reinforce 后执行，手动 reinforce 工具传空 event_ids 是合理场景。

---

## 三、死代码 / 误导代码

### 12. `memory/automatic_recall.py` `"memory.revised"` 事件类型永不触发（死分支）✅ 已修复
**文件**：`backend/aiive/memory/memory_write_service.py:678-679`
所有写路径中 revise 操作经 `_execute_supersede_or_revise` 产生的是 `memory.superseded` 事件，全代码库无任何位置 emit `"memory.revised"`。该 event_type 在向量 upsert 列表中属永远走不到的死分支（与问题 4 关联，也导致"revise 应触发专用 upsert"的意图落空）。
> **修复**：从 `_enqueue_projection` 的 upsert 条件中移除 `"memory.revised"`，消除死分支。

### 13. `memory/memory_store.py` `get_active()` 已废弃但仍保留
**文件**：`backend/aiive/memory/memory_store.py:127-129`
```python
def get_active(self) -> Sequence[MemoryRecord]:
    """DEPRECATED: use get_active_valid(). Kept for backward compat."""
    return self.get_active_valid()
```
纯转发包装，全目录未见调用方引用（`forget_memory` 等已改用 `get_active_valid`）。
> **状态**：已确认非死代码，不修复。审查时遗漏了 `builtin_tools.py:454` 和 `builtin_tools.py:472` 的两处 `store.get_active()` 调用（`_handle_forget_memory` 中 scope=all 和 scope=topic 路径）。该方法为弃用封装，但仍有实际调用方，不应删除。

### 14. `memory/memory_maintenance.py` `forget/sleep/archive` 三个方法永远返回失败（误导）✅ 已修复
**文件**：`backend/aiive/memory/memory_maintenance.py:214-238`、`backend/aiive/api/routes_memories.py:87-136`
三者均 `return {"ok": False, "error": "Deprecated: ..."}`，不执行任何实际操作。其调用方 `routes_memories.py` 的三个 API 端点（`POST /memories/{id}/forget|sleep|archive`）对外表现为 HTTP 200 但永远 `"ok": false`，用户调用后发现操作完全无效。
> **修复**：`routes_memories.py` 的三个端点已改用 `MemoryWriteService.forget()` 和 `MemoryWriteService.execute_maintenance()`，执行真实的遗忘/休眠/归档操作。

### 15. `core/action_planner.py` `plan()` 未被调用 ✅ 已修复
作为对外 API 但当前无调用点，且返回字段不完整（见问题 7），属潜在死代码/未完成功能。
> **修复**：已删除 `ActionPlanner.plan()` 方法。`ActionPlanner` 类仍保留，其 `classify_memory_signal()` 方法仍在 `agent_graph.py` 中使用。对应的 3 个测试用例已移除，保留 `AgentDecision` 模型测试（该类型仍在 `agent_graph.py` 中使用）。

---

## 四、错误处理不完善

### 16. `builtin_tools.py` `_handle_ingest_document` 打开文件未用 `with`（资源泄漏）✅ 已修复
**文件**：`backend/aiive/tools/builtin_tools.py:803`
```python
content = open(file_path).read()   # 未用 with，异常时文件句柄不释放
```
**建议**：改为 `with open(file_path, "r") as f: content = f.read()`。
> **修复**：改为 `with open(file_path) as f: content = f.read()`，异常时自动释放文件句柄。

### 17. `builtin_tools.py` `safe_delete` 工具未走注册表
**文件**：`backend/aiive/tools/builtin_tools.py:743-756`
`_handle_safe_delete` 直接调用 `safe_delete(path, scope_id, mode)` 而未传入 `registry`。该工具默认 `scope_id="test_artifacts"`，且底层 `safe_delete` 在未传 registry 时使用全局单例。结合问题 1（全局单例 REPO_ROOT 错误），实际删除范围与危险路径保护均不可靠。
> **状态**：已确认当前无需修复。问题 1（REPO_ROOT 多一层）已在当前代码中修复为 `parents[3]`，全局单例的 scope 路径和危险路径保护已正确。`safe_delete` 函数接受可选 `registry` 参数设计合理，全局单例作为默认值满足当前场景。

### 18. `routes_mcp_install.py` 冒烟测试注册 stub 却调用真实工具
**文件**：`backend/aiive/api/routes_mcp_install.py:98-113,148-172`
审查时 `_build_test_client()` 仅注册 `echo`/`list_files` 两个本地 stub，但 `smoke_capability` 直接 `client.call_tool(request.tool_name, ...)`。`request.tool_name` 若为目标 MCP 工具（如 `github_search`），会因未注册而抛错，冒烟测试实际测的是 stub 而非目标能力，与注释不符。
> **状态**：当前代码已修复此问题。`_build_test_client` 现在注册的是目标能力声明的真实工具名（`for name in declared_tools`），每个工具处理器 raise RuntimeError（诚实报告无真实 MCP 执行器）。冒烟测试验证工具声明正确性和接线完整性，不伪造成功结果。

### 19. `routes_capabilities.py` activate 分支 `capability_id` 可能为 `None` 却返回 `ok=True` ✅ 已修复
**文件**：`backend/aiive/api/routes_capabilities.py:111-130`
若 `install_sandbox` 成功但 `Capability` 记录未创建（理论上不应发生，但代码未强约束），`cap` 为 None，`plan.capability_id` 保持默认 None，接口仍返回 `ok=True` 且 `capability_id=""`，调用方无法区分"激活成功"与"安装成功但能力注册缺失"。建议对 `cap is None` 显式返回失败。
> **修复**：增加 `if cap is None` 显式检查，`plan.status` 设为 `"failed"` 并返回 `ok=False`，不再静默返回成功。

### 20. 多处裸 `except Exception` / 吞异常（汇总）✅ 已核查
- `agent_graph.py` 多处 `logger.exception(...)` 后继续，部分异常被静默忽略（如 `_finalize` 中 `TaskWorker.poll_and_notify()` 异常仅记录）。
- 多个 handler 用 `try/except Exception: return {"ok": False, ...}`，但未记录堆栈，排查困难。
- 建议：对可能影响功能正确性的异常保留堆栈日志，并区分"业务失败"与"系统异常"。
> **状态**：已逐项核查，未发现实际 bug。
> - `classify_memory_signal` 的 `except Exception: pass`：方法前已用 `MemorySignalDecision(action=EXTRACT_ASYNC)` 初始化 `signal` 默认值，异常时使用默认值，设计正确。
> - `TaskWorker.poll_and_notify()` 异常：`logger.exception(...)` 已记录完整堆栈，非静默吞异常。
> - 其他 handler 的 `except Exception` 均含 `exc_info=True` 或 `logger.exception()`，异常可追溯。
> - 建议改进项（非 bug）：部分 handler 返回 `{"ok": False}` 时可追加 `logger.warning` 方便排查。

---

## 五、接口错误 / 新旧逻辑混用

### 21. `builtin_tools.py` 自进化工具传参与实现疑似脱节 ✅ 已修复
**文件**：`backend/aiive/tools/builtin_tools.py:930-933`
```python
def _handle_apply_patch_to_inactive_slot():
    from aiive.selfdev.patch_executor import PatchExecutor
    return PatchExecutor().apply_to_inactive([])   # 传入空列表，patch 来源不明
```
`apply_to_inactive([])` 传入空 patch 列表，需确认 `PatchExecutor.apply_to_inactive` 是否从其它渠道获取补丁；若依赖参数，则此处永远无补丁可应用（功能空转）。
> **修复**：添加 `operations` 参数，更新工具 schema 注册，Agent 可将 `create_selfdev_plan` 返回的 `plan.operations` 传入。默认值为 `None` 时转为空列表保持兼容。

### 22. `routes_attention.py` 缺省响应缺字段（前端解析风险）✅ 已修复
**文件**：`backend/aiive/api/routes_attention.py:40-48`
无注意力状态时返回 `{"thread_id":..., "decision":"continue"}`，缺少 `suggestion`/`focus_topic`/`recent_topics` 等字段，前端若按完整结构解析会得到 `undefined`。
> **修复**：无状态时补充 `suggestion=None, focus_topic=None, recent_topics=[]`，与正常响应字段结构一致。

### 23. 同一功能多套端点并存（新旧逻辑混用）✅ 已核查
- 提醒取消：`builtin_tools._handle_cancel_task`（标记 `cancelled`）与 `routes_notifications.delete_notification`（物理删除）语义并存。
- 记忆查询：`memory_search`/`memory_timeline`/`memory_event_log` 三类查询并存。
> **状态**：`delete_notification` 当前代码已修复合一——物理删除 Event 同时标记关联 Task 为 `cancelled` 并更新相关 `reminder_created` 事件（见 `routes_notifications.py:86-108`）。三种记忆查询各司其职：语义搜索 / 版本时间线 / 关键词事件日志，非冗余。无需修复。

### 24. `installer.py` `capability_id` 字段赋值 ✅ 误报
`installer.py:76` 已正确传入 `capability_id=capability_id`，`MCPInstallRecord.capability_id` 与 `CapabilityVersion.capability_id` 使用主键 `cap.id` 作为外键引用属有意设计。**此条为分模块审查误报，已排除。**

---

## 六、可优化部分（非缺陷，建议改进）

### 25. 重复构建 LangGraph（每次请求编译一次）
**文件**：`backend/aiive/runtime/agent_graph.py:_build_graph` / `run_stream`
每次 `run()`/`run_stream()` 都 `StateGraph(...).compile()`。图结构（节点、边、policy 规则）与具体 `llm_with_tools` 无关，可编译为可复用图，仅 `invoke` 时传入工具。当前实现每次请求重复编译，存在性能浪费。
> **评估**：编译开销微秒级，LLM API 调用秒级，实际影响可忽略。不处理。

### 26. `_build_agent_context` 中 `import time` 放在函数体内 ✅ 已修复
**文件**：`backend/aiive/runtime/agent_graph.py:316`
`import time as _time` 在函数内，虽无害但风格不一致（其余 import 在文件头）。建议提到模块顶部。
> **修复**：`import time` 移至模块顶部，函数体内 `_time.monotonic()` → `time.monotonic()`。

### 27. `recall_config` 每轮重建
**文件**：`backend/aiive/runtime/agent_graph.py:294` 与 `memory_search` 中均 `RecallConfig()` 实例化；若配置为全局常量，建议改为模块级单例，避免重复构造。
> **评估**：`RecallConfig()` 为纯 dataclass，构造开销可忽略。不处理。

### 28. `_create_tool_result_events` 仅处理 `schedule_reminder`
**文件**：`backend/aiive/runtime/agent_graph.py:169-208`
该函数只把 `schedule_reminder` 的 tool result 持久化为 `reminder_created` 事件。其它会写入业务事件的能力（如 `remember_or_update` 是否也需要事件化）若依赖此函数则会漏掉。建议明确事件化范围或补充。
> **评估**：`remember_or_update` 等工具走 `MemoryWriteService` 自有事件系统（`_log_event`），在此函数中创建会重复。当前行为是正确设计。不处理。

### 29. N+1 / 循环内重复查询
- `memory_search`/`memory_event_log` 中按 `scope.chain()` 逐 scope 查询，scope 较多时产生多次 DB 查询，可考虑 `IN` 合并。
- `show_notifications`/`dismiss_notifications` 用 `limit(200)` 后内存遍历过滤，数据量大时效率低，建议把状态过滤下推到 SQL `WHERE`。
> **评估**：典型 scope chain 仅 3 层（thread→project→global），3 次查询开销可忽略；通知列表 `limit(200)` 在实际场景中数据量通常很小。不处理。

### 30. 重复的 assistant/policy_check/tools_node 闭包定义（代码重复）
**文件**：`backend/aiive/runtime/agent_graph.py`
非流式 `_build_graph` 与流式 `run_stream` 内各自定义了几乎相同的 `_assistant`/`_policy_check`/`_tools_node` 闭包，差异仅在流式追踪。建议抽取共享节点函数，减少重复与漂移风险。
> **评估**：~170 行 × 2 的代码重复确实增加维护成本，但闭包捕获了不同的局部变量（`tools`、`registry`、`trace_id` 等），重构需谨慎设计共享接口。建议后续在流式/非流式统一重构时一并处理。不处理。

---

## 七、总结与优先级建议

| 优先级 | 编号 | 问题 | 类型 | 状态 |
|--------|------|------|------|------|
| P0（必须修） | 2 | task_worker 未定义变量导致提醒静默失效 | 逻辑错误/功能失效 | 待修复 |
| P0 | 1 | safe_delete REPO_ROOT 多一层 → 防护失效 | 安全/逻辑错误 | 待修复 |
| P0 | 3 | policy_engine 未知工具被悄悄放行 | 安全/逻辑错误 | 待修复 |
| P0 | 4 | supersede 向量 upsert+delete 冲突 → 记忆不可检索 | 数据一致性 | 待修复 |
| P1 | 5 | 删除通知未清理 Task → 提醒复现 | 逻辑错误 | 待修复 |
| P1 | 6 | chat 路由 rollback 错误会话 | 错误处理 | 待修复 |
| P1 | 18 | MCP 冒烟测试测的是 stub | 接口错误 | 待修复 |
| P2 | 8 | attention 时区不一致 | 健壮性 | ✅ 已修复 |
| P2 | 9 | scope_score 多同类型 scope 打分错误 | 逻辑错误 | ✅ 已修复 |
| P2 | 10 | content_hash 注释不符（功能一致） | 注释 | 不修复 |
| P2 | 11 | reinforce 空事件 +1（设计如此） | 设计 | 不修复 |
| P2 | 12 | memory.revised 死分支 | 死代码 | ✅ 已修复 |
| P2 | 13 | get_active() 仍有调用方 | 误报 | 不修复 |
| P2 | 14 | MemoryMaintenance forget/sleep/archive 失效 | 逻辑错误 | ✅ 已修复 |
| P2 | 15 | plan() 死代码 | 死代码 | ✅ 已修复 |
| P2 | 16 | 文件句柄泄漏（open 未用 with） | 资源泄漏 | ✅ 已修复 |
| P2 | 17 | safe_delete 未走注册表（REPO_ROOT 已修复） | 设计 | 不修复 |
| P2 | 18 | MCP 冒烟测试 stub vs real（当前代码已修复） | 接口 | 不修复 |
| P2 | 19 | activate 返回 ok=True 但 cap 可能为 None | 错误处理 | ✅ 已修复 |
| P2 | 20 | 裸 except Exception 风险（核查后无实际 bug） | 错误处理 | 已核查 |
| P3 | 21 | apply_patch 硬编码空列表 | 逻辑错误 | ✅ 已修复 |
| P3 | 22 | attention 缺省响应缺字段 | 接口/健壮性 | ✅ 已修复 |
| P3 | 23 | delete_notification/Task 残留（已修复） | 接口一致性 | 已核查 |
| P3 | 24 | installer capability_id（误报） | 误报 | 已排除 |
| P3 | 25 | 重复编译 LangGraph（编译开销可忽略） | 性能 | 不处理 |
| P3 | 26 | `import time` 在函数体内 | 代码风格 | ✅ 已修复 |
| P3 | 27 | recall_config 每轮重建（构造开销可忽略） | 性能 | 不处理 |
| P3 | 28 | tool_result_events 仅 schedule_reminder（设计正确） | 设计 | 不处理 |
| P3 | 29 | N+1 scope 查询（典型 3 层可忽略） | 性能 | 不处理 |
| P3 | 30 | 流式/非流式闭包重复（后续统一重构时处理） | 代码重复 | 不处理 |

**备注**：
- 第 24 条（`installer.py capability_id`）为分模块审查的误报，已通过直接复读源码排除，特此说明以免重复劳动。
- 第 13 条（`get_active()`）审查时遗漏了 `builtin_tools.py` 中的实际调用方，非死代码。
- 问题 7~15 已于 2026-07-14 逐项核实：8、9、12、14、15 已修复；7、10、11、13 确认无需修复（原因见各条目标注）。
- 所有"待修复"条目建议在落地前结合运行测试核对源码，因分模块审查输出较长、部分细节未在主代理处逐行复读。
- 本次审查未运行测试/未启动服务，所有结论基于静态阅读。建议在修复 P0 项后补充对应回归测试（尤其提醒链路、安全删除 scope、policy 引擎批次判定、记忆向量投影）。
