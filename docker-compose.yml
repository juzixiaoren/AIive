services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: aiive
      POSTGRES_PASSWORD: aiive_dev
      POSTGRES_DB: aiive
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
