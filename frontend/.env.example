# 前端环境变量模板（复制为 .env 并按需修改）
# 被 git 跟踪；不含任何密钥，仅用于说明可用配置项。

# 开发者 UI 构建开关
# true  : 顶部导航栏显示「开发者」只读诊断页入口
# 其他  : 隐藏「开发者」入口（默认）
# 注意：仅开启此项只会显示导航入口；开发者页实际数据来自后端，
#       还需后端配置 AIIVE_DEVELOPER_DIAGNOSTICS_ENABLED=true 才会返回数据，
#       且后端接口仅允许本机 loopback 访问。
VITE_AIIVE_DEVELOPER_UI_ENABLED=false
